import defaultIcon from '../icons/default.svg';

export const PLATFORM_CHOICES_DEFAULT = '默认';

export const PLATFORM_NAME_ENUM_DEFAULT = 'default';

export const PLATFORM_ICON_ENUM_DEFAULT = defaultIcon;
